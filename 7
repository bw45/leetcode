class Solution:
    def reverse(self, x):
        if x < -2147483648 or x > 2147483648:
            return 0
        else:
            return int(str(x)[::-1]) if '-' not in str(x) else str(x)[0]+str(int(str(x)[1:][::-1]))
