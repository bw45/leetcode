class Solution:
    def firstMissingPositive(self, nums: List[int]) -> int:
        return next((i for i in range(1,len(nums)+2) if i not in nums ))

class Solution:
    def firstMissingPositive(self, nums: List[int]) -> int:
        return next((i for i in range(1,10000) if i not in nums))
